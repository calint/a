// sample of cap code
/// compiles to c
f=file(ram,sizeof(ram));
f.info(stdout);nl();
f.to(stdout);nl();
f.copy("another",3);
f.to(stdout);nl();

typedef size_t size;
set const size s=f.size_in_bytes();
pl("file size: %lu",s);

f.to(stdout);

__block char ch;
f.foreach_char_write(^(char*c){
    putchar(*c);
    *c='x';
    putchar(ch++);
});

f.free();