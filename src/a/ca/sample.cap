// sample of cap code
/// compiles to c
f=file(ram,sizeof(ram));
f.info(stdout);
f.to(stdout);
f.copy("another",3);
f.to(stdout);
set const size_t size=f.size_in_bytes();
pl("file size: %lu",size);
call f.to(stdout);

__block char ch;
call f.foreach_char_write(^(char*c){
    putchar(*c);
    *c='x';
    putchar(ch++);
});
rel f;