address{int}
size_in_bytes{int}
file{
  address;
  size_in_bytes;
  to(stream){pl("file %p{%d %d}",o,address.i,size_in_bytes.i)}
}

c{run(){
  int i=2;
  i=i+3;
  pl("hello world %d",i);
  pl("%s","ehllo");
  
  file f;
  f.to(out);
}}
