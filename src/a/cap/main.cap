address{int;
  to(stream){pls(s,"address: %d\n",i);}
}
size_in_bytes{int}
name{str i}
file{
  address;
  size_in_bytes;
  name;
  to(stream){
    pls(s,"file '%s' @ %p{%d %d}\n",name.i,o,address.i,size_in_bytes.i);
  }
  address get_address(){}
}

c{run(){
  file f;
  file f2;
  f.address=f2.address;
  f.address.to(out);
  f.to(out);
  f.address=0xa0000;
  f.address.i=0xa0002;
}}
