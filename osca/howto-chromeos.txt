# install base system
#sudo sh crouton -r sid -t x11,keyboard,extension

# enter chroot
#sudo enter-chroot

# validate x11 install
#xinit

# in xterm install git and get frameless
sudo apt-get install git
mkdir ~/w/a
git clone https://github.com/calint/a.git ~/w/a

# copy init files from frameless
cp -a ~/w/a/osca/etc/skel/. ~/.

# build frameless window manager
cd ~/w/a/osca/frameless/
sh make.sh

# build clonky system overview
sudo apt-get install libxft-dev
cd ~/w/a/osca/clonky/
sh make.sh 

# exit x11
#exit

# exit chroot
#exit

# re-enter chroot activating the new .bashrc 
#sudo enter-chroot

# start x11
#xinit .xsession

# init aliases
alias ~/.aliases

# install iostat
pki sysstat

# install browsers
pki chromium-browser
pki firefox

# install image viewer
pki feh

# install screenshot taker
pki scrot

# install lightweight editor
pki leafpad

# build launcher
cd ~/w/a/osca/menuq/
sh make.sh 

# install cli
pki lxterminal

# install file manager
pki thunar

