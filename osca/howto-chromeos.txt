# install base system
#sudo sh crouton -r sid -t x11,keyboard,extension

# enter chroot
#sudo enter-chroot

# validate x11 install
#xinit

# in xterm install git and get frameless
sudo apt-get -y install git
mkdir ~/w/a
git clone https://github.com/calint/a.git ~/w/a

# copy init files from frameless
cp -a ~/w/a/osca/etc/skel/. ~/.

# build frameless window manager
cd ~/w/a/osca/frameless/
sh make.sh

# build clonky system overview
sudo apt-get -y install libxft-dev
cd ~/w/a/osca/clonky/
sh make.sh 

# exit x11
#exit

# exit chroot
#exit

# re-enter chroot activating the new .bashrc 
#sudo enter-chroot

# start x11
#xinit .xsession

# install iostat, browsers, image viewer, screenshot taker, editor, cli, filemanager
sudo apt-get -y install sysstat chromium-browser firefox feh scrot leafpad lxterminal thunar

# build launcher
cd ~/w/a/osca/menuq/
sh make.sh 
